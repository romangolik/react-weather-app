[build]
  command = "npm run build:prod"
  publish = "build"

[dev]
  command = "npm run dev"
  targetPort = 3000
  publish = "dist"

[[redirects]]
  from = '/api/*'
  to = '/.netlify/functions/:splat'
  status = 200